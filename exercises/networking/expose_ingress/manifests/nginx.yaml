apiVersion: v1
kind: Pod
metadata:
  name: nginx
  labels:
    app: nginx
spec:
  containers:
  - image: nginx
    name: nginx
    imagePullPolicy: IfNotPresent
    name: nginx
  restartPolicy: Always